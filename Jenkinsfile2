echo "---build start---"

stage('Checkout Stage') {
  echo "---Checkout---"
}

stage('Build Stage') {
  echo "---Build Stage---"
    //docker container ls
  echo "${P_VERSION}"
}

stage('Push Stage') {
  echo "---Push Stage---"
}
